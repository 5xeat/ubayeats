<h2>編輯用戶資料 <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, "用戶信箱" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :name, "用戶名稱" %><br />
    <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
  </div>

  <div class="actions">
    <%= f.submit "送出" %>
  </div>
<% end %>

<h3>註銷帳號</h3>

<p>提交申請，刪除所有數據，提交申請，刪除所有數據，永久註銷UbayEats帳號 <%= button_to "註銷帳號", registration_path(resource_name), data: { confirm: "帳號註銷後，您將無法繼續享受UbayEats服務，是否確認要註銷帳號？" }, method: :delete %></p>

<%= link_to "Back", :back %>
